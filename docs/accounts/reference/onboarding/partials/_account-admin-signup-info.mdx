:::info Account admin fields and sign-up
Although many `accountAdmin` fields are optional during onboarding, they're required when the user signs up. Swan recommends collecting and sending this information before the end of the onboarding flow.
:::
