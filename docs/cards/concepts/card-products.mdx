---
title: Card products
audience:
  - dev
  - ops
related:
  - /cards/concepts/card-packages
  - /cards/concepts/design
  - /cards/concepts/physical/delivery-and-shipping
---

import CardProductExplanation from '../../_shared/definitions/_card-product.mdx';
import FinnishLimitations from '../../_shared/partials/_finnish.mdx';

# Card products

<p className="ia-lede">A reusable collection of design, settings, and package that defines the cards you offer, identified by a <code>cardProduct.id</code>.</p>

<CardProductExplanation />

Learn more about designing your card products in the [card design section](/cards/concepts/design).

:::info Printing hub
The [printing hub](/cards/concepts/physical/delivery-and-shipping#hubs), where physical cards are printed then shipped to your cardholders, is defined in the card product ID.
You must have one dedicated card product per hub, though the designs can be identical.
:::

Each card product includes a [card package](/cards/concepts/card-packages): a bundle of features, insurance, and pricing that applies to every card you issue, letting you quickly launch market-ready offers without building everything from scratch.
[Insurance coverage](/cards/concepts/card-packages#insurance) to protect cardholders is determined by the selected card package tier.

## Card language {#language}

Your card's language (the language that appears on payment terminals, for example) is usually the same as the language chosen for the Swan account.

For Swan's no-code and open source Web Banking interface, the card language is determined by the default language of the browser where the card is created.

When using the API, the default language is the [account language](/accounts/concepts/account/language).
You can update the default language by calling the `bindAccountMembership` mutation and adding `accept-language` to the HTTP header.
This lets users choose their card language.

:::info Finnish (`fi`)
<FinnishLimitations />
:::
