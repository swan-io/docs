---
title: MCP server
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# MCP server

Swan's MCP server gives AI assistants direct access to Swan's GraphQL API schema. Connect your AI coding tools to get accurate help with queries, mutations, and schema exploration.

## Available tools

| Tool | Description | Example prompts |
|------|-------------|-----------------|
| <nobr>`search-graphql-schema`</nobr> | Search the schema using keywords to find relevant types, fields, and operations. | • "Find all types related to payments"<br/>• "What fields are available for accounts?"<br/>• "Show me SEPA credit transfer mutations"<br/>• "Search for card-related types" |
| <nobr>`introspect-graphql-type`</nobr> | Get detailed information about specific GraphQL types, including fields and related types. | • "What is the Account type?"<br/>• "Show me all fields on the Transaction type"<br/>• "How do I create a SEPA credit transfer?"<br/>• "Explain the AccountMembership type and its relationships" |

## Setup

Add the Swan MCP server to your AI coding tool's configuration.

:::note
Swan's MCP server only supports the Streamable HTTP transport protocol.
:::

<Tabs groupId="mcp-config">
<TabItem value="cursor" label="Cursor">

Add to your Cursor MCP configuration file:
- **macOS/Linux**: `~/.cursor/mcp.json`
- **Windows**: `%USERPROFILE%\.cursor\mcp.json`

```json
{
  "mcpServers": {
    "swan-graphql": {
      "url": "https://mcp.swan.io/graphql/mcp"
    }
  }
}
```

After saving the configuration, enable the server in **Cursor Settings** > **MCP**.

Refer to [Cursor's MCP documentation](https://docs.cursor.com/context/model-context-protocol) for more information.

</TabItem>
<TabItem value="claude-desktop" label="Claude Desktop">

Add to your Claude Desktop configuration file:
- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Linux**: `~/.config/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`

```json
{
  "mcpServers": {
    "swan-graphql": {
      "url": "https://mcp.swan.io/graphql/mcp"
    }
  }
}
```

Restart Claude Desktop after saving the configuration.

Refer to [Claude Desktop's MCP guide](https://modelcontextprotocol.io/quickstart/user) for more information.

</TabItem>
<TabItem value="claude-code" label="Claude Code">

Run the following command:

```bash
claude mcp add --transport http swan-graphql https://mcp.swan.io/graphql/mcp
```

Refer to [Claude Code's MCP documentation](https://docs.anthropic.com/en/docs/claude-code/mcp) for more information.

</TabItem>
<TabItem value="other" label="Other clients">

Use the following settings to configure any MCP-compatible client:

- **URL**: `https://mcp.swan.io/graphql/mcp`
- **Transport**: Streamable HTTP

Refer to your client's documentation and the [Model Context Protocol specification](https://modelcontextprotocol.io/) for setup instructions.

</TabItem>
</Tabs>

## Next steps

- [Learn about Swan's GraphQL API](../graphql.mdx)
- [Explore the API with API Explorer](./api-explorer.mdx)
- [Test your integration in Sandbox](./sandbox-users.mdx)