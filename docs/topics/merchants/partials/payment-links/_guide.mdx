1. Call the `createMerchantPaymentLink` mutation (line 2).
1. Add the `merchantProfileId` (line 4).
1. Add the `amount`, including both the `value` and `currency` (line 5).
1. Add your {props.method} `paymentMethodId` (line 6). {props.note}
1. Include a `label` and `reference` if you'd like (lines 7-8). The `label` can't exceed 140 characters. Note that the `label` might appear on the {props.method === 'direct debit' ? 'debtor\'s bank' : 'payer\'s card'} statement.
1. Add the success payload (line 11). Include the final `url` in the payload if you'd like (line 14).
1. Add rejections (not shown).