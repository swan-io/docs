---
title: Enable a payment mandate
---

# Enable a payment mandate

Enable a SEPA Direct Debit received payment mandates with the API.

[From old doc](https://docs.swan.io/concept/payment/direct-debit#re-enablement-of-a-received-sepa-direct-debit-mandate): A received SEPA direct debit mandate with Suspended status can be switched back to its last status (Enabled for SEPA Direct Debit Core mandates, Enabled or ConsentPending for SEPA Direct Debit B2B mandates) by any accountMembership with CanInitiatePayments rights on the account to which the received SEPA direct debit mandate is associated using the enableReceivedDirectDebitMandate mutation.

## Guide {#guide}

1. Call the `enableReceivedDirectDebitMandate` mutation.
1. Add your ....
1. Add the payload and rejection.

## Mutation {#mutation}

🔎 [Open the mutation in API Explorer]

```graphql {2} showLineNumbers

```

## Payload {#payload}

The payload shows the ... (line 7).

```json {5} showLineNumbers

```