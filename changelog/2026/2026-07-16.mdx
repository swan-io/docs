---
title: 16 July 2026
type: release
---

## New features

💳 You can now stop a physical card from renewing automatically, without canceling it. Call the new `cancelPhysicalCardRenewal` mutation while the card is in the `ToRenew` status. The card stays active until its natural expiration date, then expires instead of being reissued.

## Improvements

📦 You can now add several cards to Apple Pay or Google Pay in one call, with a single consent flow. The new `addDigitalCards` mutation works like `addDigitalCard`, but adds multiple cards at once.
