:::tip Prerequisites
* You must have a [project access token](/build/using-api/authentication#tokens-project).
* The onboarding must be ongoing, meaning its status is `Valid` or `Invalid` (not `Finalized`).
:::