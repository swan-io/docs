:::tip Prerequisites
To update an onboarding:

* You must have a [project access token](https://docs.swan.io/api/authentication/project-access-token).
* The onboarding status must be `Ongoing` (valid or invalid).
:::