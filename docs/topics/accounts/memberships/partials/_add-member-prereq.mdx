:::tip Prerequisites
To add new members, you must authenticate with a [user access token](../../../authentication/index.mdx#tokens-user) associated with an existing account member with the permission `canManageAccountMembership`.

Only account members with the permission `canManageAccountMembership` can add account members.
:::