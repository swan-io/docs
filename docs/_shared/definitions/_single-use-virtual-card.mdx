A virtual card restricted to a single transaction (one-off) or a single dedicated merchant (recurring).
Because of their limited scope, users can avoid 3-D Secure (3DS) consent when paying with single-use virtual cards.
