---
title: Individual onboarding fields
related:
  - /accounts/guides/onboarding/individual
  - /accounts/reference/onboarding/country-requirements
  - /accounts/reference/onboarding/company-onboarding-fields
audience:
  - dev
  - ops
---

# Individual onboarding fields

<p className="ia-lede">Reference tables for the fields collected when creating an individual onboarding, including required, optional, and conditional rules per country.</p>

Use this reference to determine which fields are required, optional, or conditional for each country when creating an individual onboarding.
For GraphQL types and validation constraints, refer to [`CreateIndividualAccountHolderOnboardingInput`](https://api-reference.swan.io/inputs/create-individual-account-holder-onboarding-input/) in the API reference.

import SupportingDocumentsCaution from './partials/_supporting-documents-caution.mdx';

<SupportingDocumentsCaution />

<FieldRequirementsLegend />

import IndividualCoreFields, {toc as coreToc} from './partials/_individual-core-fields.mdx';

<IndividualCoreFields />

## OAuth redirect parameters {#oauth-redirect-parameters}

**Related field:** `oAuthRedirectParameters`

import OAuthRedirectParameters from './partials/_oauth-redirect-parameters.mdx';

<OAuthRedirectParameters />

## No-code Web Banking {#no-code-web-banking}

If you use Swan's no-code Web Banking, the following fields are collected during the onboarding and sign-up flows.

- `accountAdmin.firstName`, `lastName`, `nationality`.
- `accountAdmin.address`: `addressLine1`, `city`, `country`, `postalCode`.
- `accountAdmin.birthInfo`: `birthDate`, `city`, `country`, `postalCode`.
- `accountAdmin.unitedStatesTaxInfo`.

## API mapping {#api-mapping}

import IndividualApiMapping, {toc as mappingToc} from './partials/_individual-api-mapping.mdx';

<IndividualApiMapping />

export const toc = [
  ...coreToc,
  { value: 'OAuth redirect parameters', id: 'oauth-redirect-parameters', level: 2 },
  { value: 'No-code Web Banking', id: 'no-code-web-banking', level: 2 },
  { value: 'API mapping', id: 'api-mapping', level: 2 },
  ...mappingToc,
];
