:::note Can manage cards
The `canManageCards` permission is technically optional, but it's recommended to choose `true` or `false` anyway.
If you don't include it in your API call, [it inherits the same value as `canManageAccountMembership`](/accounts/reference/memberships/membership-permissions#permissions-cards).
:::
